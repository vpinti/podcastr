const authConfig = {
    providers: [
        {
            domain: "https://evident-jaybird-58.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
